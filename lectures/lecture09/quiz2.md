# Quiz 2: part 1 recap

## Q1

A web **server**:

a. is a hardware device
b. listens for incoming requests
c. must be implemented in JavaScript
d. terminates after handling a request

## Q2

A web **client**:

a. listens for incoming responses
b. can run Python code
c. sends a request, then waits for a response
d. is always connected to a server

## Q3

In HTTP, `GET` and `POST` are examples of:

a. request methods
b. client actions
c. web protocols
d. status codes

## Q4

In HTTP, `200 OK` and `404 Not Found` are examples of:

a. request methods
b. error messages
c. status codes
d. return values

## Q5

In HTTP, **headers** are used for:

a. indicating the beginning of a request
b. communicating metadata between client/server
c. encrypting messages
d. delivering a message payload
